<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="@Url.Action("Index", "Home")" onclick="trackNavigation('home')">CSPGuardian Demo</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" 
                onclick="toggleMobileMenu()" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Home")" onclick="trackNavigation('home')">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Products")" onclick="trackNavigation('products')">Products</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Orders")" onclick="trackNavigation('orders')">Orders</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Dashboard", "Admin")" onclick="trackNavigation('admin')">Admin</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<script>
    function trackNavigation(page) {
        // Analytics tracking
        console.log('Navigation to:', page);
        if (typeof gtag !== 'undefined') {
            gtag('event', 'page_view', {
                'page_title': page,
                'page_location': window.location.href
            });
        }
    }
    
    function toggleMobileMenu() {
        var nav = document.getElementById('navbarNav');
        nav.classList.toggle('show');
    }
</script>

<style>
    .navbar {
        margin-bottom: 20px;
    }
    
    .nav-link {
        cursor: pointer;
    }
    
    .nav-link:hover {
        background-color: rgba(255,255,255,0.1);
    }
</style>

